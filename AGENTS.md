# 弦外小猫 (KittyEcho) — 项目规则

本工程 = FlorisBoard 基底改造的安卓中文语音输入法「弦外小猫」(applicationId `com.wordtaker.keyboard`)。

## 规则真源（强制）

1. 根目录 `AGENTS.md` 是本项目唯一规则真源；后续所有项目任务均以本文件为准。
2. 后续不得把 `CLAUDE.md` 视为项目规则，也不得从中加载或推导项目约束。

## 沟通与需求澄清（强制）

1. 每次对话/任务完成后，用最简洁的语言汇报结果；需要协助或提问时同样极简。
2. 任何经证据检查仍无法消除的需求歧义，必须向用户提出具体、最少必要的问题，并持续确认直至明确；等待用户回答期间，可继续执行不受该歧义影响的明确项。禁止以“暂缓”代替澄清，也禁止猜测实施。

## 子 agent 编排纪律（强制）

1. 子 agent 必须是禁止再次转派的叶子执行者，并在明确范围内完成工作、自测和回报。
2. 查询状态、读取少量文件、单次构建和简单配置等小事由主循环直接完成；同一调研不重复进行。
3. 同一 Gradle 工程的构建必须串行并在前台运行；只有真正独立的任务可以并行。
4. 若 agent 空转、只转派或没有实际产出，立即停止并由主循环接管。

## Codex 上下文分支体系（强制）

1. 固定且长期复用的上下文仅 5 个：主干（总协调）、需求与设计决策、Android 主实现、质量与发布、其他。
2. 新需求默认不创建新上下文：歧义或方案权衡交由需求与设计决策；常规开发或修复交由 Android 主实现；测试、构建、版本或交付交由质量与发布；无法归类的小型低耦合事项交由其他；主干只负责路由、协调和复核。
3. 仅“大型需求”允许新建临时专用上下文。满足任一条件即属大型：跨越至少两个核心领域且需要独立所有权；包含独立数据迁移、支付、认证、云端或架构改造；预计需要多阶段实施与独立验收；与当前主实现并行能显著缩短周期且文件边界清晰。单一页面、单个 Bug、普通 UI 或设置、轻量调查均不得新建。
4. 创建前必须先证明四个常驻执行分支均不适合；优先续用原分支，禁止因一轮新需求或分支空闲而创建“替代”“修复”“本轮迭代”窗口。仅原分支确实不可恢复时才可建立替代，并立即归档旧分支。
5. 临时大型分支完成并经主干验收后立即归档；一次迭代的版本与最终验收仍由常驻质量与发布分支处理，不另开发布分支。
6. 所有新建临时上下文统一使用 GPT-5.6 Sol、xhigh；这里的分支是 Codex 上下文而非 Git 分支；共享工作树中的 Gradle 构建继续串行。
7. “其他”只处理小型且无法归类的事项；发现任务实际属于常驻分支或达到大型标准时，必须退回主干，不得擅自继续，也不得再创建子上下文。

## 大任务清单的完成纪律（强制）

1. 建立可追踪清单，按「调研 → 分模块实现与构建 → 全面测试 → 打包」推进并及时勾选。
2. 阶段间记录进度，结束前按清单逐项验收；只能真机验证的项目必须明确标注。
3. 最终按「已完成 / 待办 / 需真机」简要汇报。

## 测试闭环（强制）

每次需求、修复或其他改动完成后：

1. 生成覆盖正向、边界、异常和回归场景的测试用例。
2. 在真实环境执行；Android 优先使用模拟器，并对候选上屏、真实中文语音 ASR 等受限能力进行真机验证。
3. 修复失败项后重新生成受影响用例并重跑，直到通过。
4. 生成用例、执行和修复可分别交给新的叶子 agent，但仍须遵守上文的禁止递归转派与串行构建规则。
5. 最多循环 10 次；仍有问题时停止并极简汇报剩余问题和已完成轮次。

## 安全与敏感信息（强制）

1. 禁止将口令、API Key、访问令牌、私钥或其他凭据写入规则、源码、日志或版本库。
2. 签名 keystore 位于本地 `app/wordtaker-release.jks`；别名和口令必须从未提交的本地 Gradle 配置或环境变量读取。

## PC 端运行保护（强制）

1. PC 端弦外小猫/WordTaker 正在日常使用时，Android、ASR 或 QA 任务严禁以 `pkill`、`killall`、quit、terminate 或其他方式停止 WordTaker、弦外小猫、WordTakerInputMethod、WordTakerBridgeMock 及其 FunASR 子进程，严禁切换或移除 macOS 输入源。
2. 不得为腾出内存、端口或读取 PC 模型而关闭用户实例；PC 项目仅可只读参考。benchmark 必须使用独立进程、仓库外独立临时目录、独立 PID 和独立端口，不得复用用户服务。
3. Android 真机或模拟器相关任务只可操作对应 Android 目标，不得影响 PC 端应用、输入法、桥接服务或其子进程。
4. 若确实无法绕开停止用户实例或切换输入源，必须暂停任务，明确说明原因与影响，并取得用户明确同意后方可继续。

## 构建要点

- `export JAVA_HOME=/opt/homebrew/opt/openjdk@17`
- `export ANDROID_HOME=/Users/Admin/Library/Android/sdk`
- Debug: `./gradlew :app:assembleDebug`；Release: `./gradlew :app:assembleRelease`
- 版本单一真源: `gradle.properties` 的 `projectVersionName` / `projectVersionCode`；每次出包按 SemVer 升 versionName 并 +1 versionCode，更新 CHANGELOG。
- ABI 仅 arm64-v8a；模型/词库/`.dat`/`.sqlite3`/`.hwr` 须 `noCompress`（native fd/mmap 加载）。
- APK 输出名 `KittyEcho-<ver>-<buildType>.apk`。
- 注意：模拟器合成触摸不触发 FlorisBoard 的 Compose 键盘候选管线，拼音/形码/手写“可视上屏”、真实中文语音 ASR 都须**真机**最终验证。
